# gh-pages-preview

Identical to `facelift-and-ve-lock` except that **CNAME is removed**.

`CNAME` claims `parcradio.net`, which the upstream repo (`parctesting/beta`)
already serves. Publishing this fork's Pages with that file would either be
rejected or redirect away from the preview, so it is dropped here.

Use this branch for the fork's GitHub Pages test at
`https://collinpikeusa.github.io/parc-website-beta/`.

**Do not merge this branch upstream** — the PR branch is `facelift-and-ve-lock`,
which keeps CNAME so production continues to serve parcradio.net.
