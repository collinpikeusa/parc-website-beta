# radiotest.org — STAGING copy of parcradio.net.
#
# Every page carries <meta name="robots" content="noindex, nofollow">.
# Crawling is deliberately ALLOWED so that tag can actually be read: a
# Disallow here would stop the crawl before the noindex is seen, and URLs
# could sit in the index with no way to remove them.
User-agent: *
Allow: /
Disallow: /ve/files/
